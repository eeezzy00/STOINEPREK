# Despot's Arena — Полное состояние проекта

> Документ собран автоматическим аудитом кодовой базы 28.08.2026.
> Все цифры взяты из реальных файлов, не из планов и не из переписки.
> При расхождении с `CLAUDE.md` — **этот файл актуальнее**.

---

## 1. Что это за игра

2D action-платформер с боевой системой на парированиях. Референс по управлению и ощущениям — **Katana Zero**. Визуал сместился от изначального «dark fantasy» к **неоновому ночному городу** (розово-фиолетовая палитра, самураи, синтвейв).

- **Движок:** Godot 4.7.2 stable, рендер Forward+, драйвер D3D12
- **Спрайты:** пиксель-арт, `default_texture_filter = 0` (nearest), включён snap 2D transforms/vertices — пиксель-перфект настроен
- **Разрешение:** stretch mode `canvas_items`, aspect `expand`
- **Физика:** `physics_interpolation = true`

**Как ведётся разработка:** пользователь — не программист, работает через AI-ассистента. Код пишет ассистент, пользователь ставит задачи, тестирует и принимает решения по геймдизайну. Язык общения — русский.

---

## 2. Текущий боевой баланс (реальные значения из кода)

### Игрок — `player.gd`

| Параметр | Значение | Примечание |
|---|---|---|
| `max_hp` | 3.0 | |
| `attack_damage` | 1.0 | |
| `attack_speed` | **0.5 с** | максимум 2 удара/сек |
| `strike_range` | **50 px** | ширина хитбокса |
| `HITBOX_HEIGHT` | 60 px | константа, синхронизирована с debug_visualizer |
| `move_speed` | 250 px/с | |
| `attack_startup` | 0.15 с | задержка до активных кадров |

**Прыжок:**

| Параметр | Значение |
|---|---|
| `jump_velocity` | 445 px/с |
| `jump_gravity` | 900 px/с² |
| `max_fall_speed` | 1400 px/с |
| `coyote_time` | 0.1 с |
| `jump_buffer_time` | 0.1 с |

Пиковая высота прыжка = `445² / (2 × 900)` ≈ **110 px**.

**Перекат:**

| Параметр | Значение |
|---|---|
| `dash_duration` | 0.2 с |
| `dash_distance` | **60 px** |
| `dash_cooldown` | 2.0 с |
| `double_tap_window` | 0.3 с |

Перекат можно вызвать двумя способами: клавишей `dash` или **двойным нажатием** влево/вправо.

**Парирование:**

| Параметр | Значение |
|---|---|
| `parry_late_limit_frames` | 10 кадров |
| `max_parry_charges` | 3 |
| `parry_regen_interval` | 3.0 с |
| `perfect_streak_for_bonus` | 3 |
| `normal_hit_hitstop_frames` | 2 |

**Формула отката (актуальная):**
```gdscript
knockback = maxf(12.0, 55.0 - (frames_late / 10.0) * 40.0)
```
Диапазон **12–55 px**. Раньше был 15–80 px, но с настоящей гравитацией такой откат сбрасывал игрока с платформы — сузили осознанно.

**Градация парирований:**

| Опоздание | Тир | Hitstop | Стоимость заряда | Искры (цвет / кол-во) |
|---|---|---|---|---|
| ≤ 2 кадра | PERFECT | 6 кадров | 0 | золотой / 50 |
| ≤ 5 кадров | GOOD | 4 кадра | 1 | белый / 34 |
| ≤ 8 кадров | LATE | 2 кадра | 1 | серый / 24 |
| ≤ 10 кадров | BARE | 1 кадр | 1 | тёмно-серый / 16 |
| > 10 кадров | MISS | — | — | урон игроку |

Проверка парирования **симметричная**: сравнивается момент активных кадров игрока с моментом активных кадров врага, `absf(now - enemy_active_time) * 60`. Опоздать и поторопиться — одинаково.

Три PERFECT подряд → `_next_hit_bonus = true`, следующий удар **×2 урона**, спрайт подсвечивается золотым.

### Враг (самурай) — `samurai_npc.gd`

| Параметр | Значение |
|---|---|
| `max_hp` | **3.0** |
| `attack_damage` | 1.0 |
| `attack_cooldown` | 1.1 с |
| `attack_startup` | 0.15 с |
| `attack_range` | 46 px |
| `move_speed` | 140 px/с |
| `detect_range` | 260 px |
| `jump_velocity` | 420 px/с |
| `jump_gravity` | 900 px/с² |

**Уклонение (Dodge):**

| Параметр | Значение |
|---|---|
| `dodge_distance` | 60 px |
| `dodge_duration` | 0.15 с |
| `dodge_cooldown` | 1.5 с |
| `dodge_chance` | 0.35 |
| `dodge_danger_range` | 90 px |

**Фланг (Flank):**

| Параметр | Значение |
|---|---|
| `flank_chance` | 0.3 |
| `flank_range` | 160 px |
| `flank_land_offset` | 40 px |
| `flank_check_interval` | 1.0 с |

**Отряд (Squad):** враги координируются — `_find_engaged_ally_below()`, `_is_nearest_holder_for()`, `_should_hold_position()`. Есть `reinforce_hp_ratio = 0.5`: подкрепление приходит когда союзник на половине HP. `lower_level_y_threshold = 40` — распознавание врагов на другом этаже.

**Платформинг ИИ:** враг умеет прыгать через пропасти. `edge_check_ahead = 24`, `edge_check_depth = 60`, `max_jump_gap = 200`.

### Производные (расчёт)

- **TTK игрока по врагу:** 3 HP ÷ (1 урон / 0.5 с) = **1.5 с** чистого урона
- **TTK врага по игроку:** 3 HP ÷ (1 урон / 1.1 с) = **3.3 с**
- **Разница скоростей:** 250 / 140 = **1.79×** в пользу игрока
- **Прыжок (110 px) vs рост врага:** перепрыгнуть удаётся, плюс работает фазовый проход (см. ниже)

---

## 3. Система коллизий

**Слои:**
- Слой 1 — игрок
- Слой 2 — враги
- Слой 4 — стены/земля

**Маски:**

| Состояние | `collision_mask` | `collision_layer` |
|---|---|---|
| Игрок обычный | 6 (враги + стены) | 1 |
| Игрок «фазовый» | 4 (только стены) | **0** |
| Враг обычный | 7 | 2 |
| Враг в уклонении | 4 | 0 |

**Ключевой механизм — фазовый проход.** Во время **переката И прыжка** игрок обнуляет и маску, и слой. Обнуление слоя обязательно: Godot проверяет коллизии независимо с каждой стороны, и если оставить `collision_layer = 1`, враг всё равно упрётся в игрока. Реализовано через счётчик `_phase_depth`, чтобы перекат и прыжок могли пересекаться и не восстанавливали коллизию раньше времени.

**Итог для игрока:** в NPC упираешься при ходьбе, **проходишь насквозь при прыжке и при перекате**.

**Анти-стакинг.** `_resolve_character_stacking()` есть и у игрока, и у врага: два `CharacterBody2D` могут застрять друг на друге в устойчивом равновесии. Метод ловит это по нормали коллизии и толкает верхнее тело вбок со скоростью `stack_push_speed = 140`.

---

## 4. Управление

| Действие | Клавиши |
|---|---|
| Движение влево | `A` / `←` |
| Движение вправо | `D` / `→` |
| Вверх | `W` / `↑` |
| Вниз | `S` / `↓` |
| Атака | `ЛКМ` / `Пробел` |
| Прыжок | **`J`** |
| Перекат | **`Left Shift`** или двойное нажатие `A`/`D` |

> **Важно:** прыжок на `J`, а не на пробеле — пробел занят атакой. Это стоит пересмотреть при первом плейтесте с посторонним человеком.

### Отладочные хоткеи

| Клавиша | Что делает |
|---|---|
| `F1` | Debug Visualizer — показ хитбоксов и дистанций |
| `F2` | AutoPlay Bot вкл/выкл |
| `F3` | Запуск batch-прогона ботом |
| `F5` | Админ-панель (живая настройка баланса) |
| `F6` | Сохранить пресет |
| `F7` | Загрузить пресет |
| `Esc` | Закрыть окно сохранения |

---

## 5. Архитектура — автозагрузки

В `project.godot` десять синглтонов:

| Автозагрузка | Назначение |
|---|---|
| `TelemetryLogger` | Логирует каждое действие в файл |
| `RunAnalyzer` | Сводка по завершённому рану |
| `DebugVisualizer` | Отрисовка хитбоксов (F1) |
| `AutoPlayBot` | Бот-автопрохождение для сбора данных (F2/F3) |
| `AdminPanel` | Живая настройка баланса + пресеты (F5) |
| `SettingsManager` | Настройки звука, сохранение в конфиг |
| `MusicManager` | Управление музыкой между сценами |
| `PhantomCameraManager` | Аддон камеры |
| `DialogueManager` | Аддон диалогов |
| `_mcp_game_helper` | Runtime-мост Godot AI (MCP) |

**Что логирует телеметрия:** атаки (направление, дистанция, попал/промах, HP цели), парирования (тир, откат), перекаты (позиция, кулдаун), прыжки (высота), hitstop (длительность, причина), смерти (причина: `defeated_by_enemy` / `fell_off_map`).

---

## 6. Админ-панель (F5)

Живая настройка баланса **без перезапуска игры**.

- Слайдеры по параметрам игрока и NPC (`PLAYER_SLIDERS`, `NPC_SLIDERS`)
- Тумблеры: неуязвимость игрока, неуязвимость NPC, режим AFK для NPC
- Кнопка спавна врага (`SPAWN_OFFSET = 220`)
- Кнопка сброса к дефолтам
- **Пресеты** сохраняются в `res://BAG/admin_presets`
- Быстрый переход между уровнями
- Переключение музыкальных треков

Пока панель открыта, `_physics_process` игрока замораживается — иначе клик по слайдеру засчитался бы как атака (ЛКМ = атака).

**Живая настройка требует сеттеров.** `set_strike_range()` и `set_max_hp()` существуют потому, что присвоение `strike_range` напрямую не меняет уже созданный `CollisionShape2D`. Любой новый настраиваемый параметр, влияющий на построенные ноды, нуждается в таком же сеттере.

---

## 7. Аудио

Три шины: `Master` → `Music`, `SFX`, `Ambience`.

| Событие | Файл |
|---|---|
| Атака игрока | `Audio/Katana Fx For Playyer/Katana_whoopfx_playeer.wav` + `..._second.wav` (чередуются) |
| Атака NPC | `Audio/Katana Fx For NPC/Katana_whoop_npc1.0.wav` + `... second hit.wav` |
| Парирование | `Audio/Parry Fx/Par_fx.wav` |
| Перекат | `Audio/Dash Fx For Both/Dash_whoop_fx.wav` + `Dash_whoop_fx2.wav` |
| Смерть NPC | `Audio/Death Fx/Death_fx_npc-dead.wav` + `...dead2.wav` |
| Ходьба | `Audio/Walk Fx/walksoundforplayer.mp3` (зациклен) |
| Эмбиент города | `Audio/Ambience/CITY_SOUNDS_AMBIUNCE.mp3` |
| Музыка уровня 1–2 | `Audio/Song for lvl1/LVL1NDLVL2SONG.mp3` |
| Музыка меню | `Audio/Song for menu/GAMEDEV@mainthemevol1.mp3` |

**Неиспользуемые дубли** (остались от ранних итераций, можно удалить): `Audio/Walk Fx/WALK.wav`, `Audio/Ambience/ambience background city.wav`, `Audio/Song for lvl1/Song for lvl1.wav`.

Звук ходьбы играет только при движении по земле — во время атаки, переката, прыжка и смерти явно останавливается.

---

## 8. Сцены и файлы

### Игровые сцены

| Файл | Что это |
|---|---|
| `men.tscn` / `men.gd` | Главное меню |
| `settings.tscn` / `settings.gd` | Настройки |
| `level.tscn` / `level.gd` | Уровень 1 — улица города |
| `level2.tscn` | Уровень 2 |
| `level3.tscn` | Уровень 3 (самый большой, 40 КБ) |
| `player.tscn` / `player.gd` | Игрок |
| `samurai_npc.tscn` / `samurai_npc.gd` | Враг-самурай |
| `hit_effect.tscn` | Эффект попадания |
| `street_lamp.tscn`, `road_sign.tscn` | Декор улицы |
| `template_hero.tscn` | Шаблон персонажа |
| `back.tscn`, `parallax_2d.tscn`, `parallax_background.tscn` | Фоны |

### Шейдеры

- `menu_dither.gdshader` — дизеринг в меню
- `shaders/crt_overlay.gdshader` — CRT-развёртка поверх экрана

### Генерация уровня — `level.gd`

Уровень **строится процедурно в коде**, не расставлен вручную:

| Константа | Значение |
|---|---|
| `ROAD_MIN_X` / `ROAD_MAX_X` | −1440 / 2880 |
| `ground_y` | 724 |
| `SIDEWALK_TOP` / `SIDEWALK_BOTTOM` | 700 / 745 |
| `LAMP_Y`, `LAMP_SPACING` | 745, каждые 420 px |
| `death_y` | 1400 (падение = смерть) |
| Лимиты камеры | left −1480, right 2920, top −600, bottom 900 |

Генерируется: фонари, дорожная разметка, пешеходные переходы, швы тротуара, дорожные знаки. Есть цепочка уровней через `next_level_path`, экраны победы и смерти.

**Палитра уровня:** акцент `Color(1, 0.18, 0.66)` — неоновый розовый. Смерть — `Color(0.92, 0.15, 0.15)`. Приглушённый текст — `Color(0.62, 0.42, 0.62)`.

### Аддоны

- `godot_ai` — MCP-мост, даёт ассистенту прямой доступ к редактору
- `phantom_camera` — продвинутая камера
- `dialogue_manager` — система диалогов (**подключён, но ещё не используется в игре**)

### Мусор в репозитории

Папка `Brawler Heat/` — 1048 файлов, дамп ассетов движка Ikemen GO (клон M.U.G.E.N). Попала случайно при импорте, к проекту отношения не имеет. **Кандидат на удаление.**

---

## 9. Хронология разработки

**Этап 1 — Фундамент**
Выбран Godot после отказа от Telegram Mini App. Установлены Godot 4.7, VS Code, GitHub Desktop, Aseprite, ComfyUI.

**Этап 2 — Подключение AI к редактору**
Пробовали Godot MCP Pro (WebSocket — не совместим с Claude Code CLI), затем `ee0pdt/Godot-MCP`. В итоге встал **Godot AI** — работает, ассистент видит дерево сцены и делает скриншоты.

**Этап 3 — Первый уровень**
Параллакс-фон ночного города (6 слоёв), земля, игрок с движением и анимациями, камера следования.

**Этап 4 — Боевая система**
Атака с регистрацией в активных кадрах (не по клику), парирование со скользящим окном, перекат с неуязвимостью и afterimage, hitstop, искры, счётчик стрика.

**Этап 5 — Инструменты разработки**
Телеметрия, анализатор ранов, визуализатор хитбоксов, бот-автопрохождение, админ-панель с пресетами.

**Этап 6 — Полировка и контент**
Свой аудио-пак, три уровня, меню и настройки, CRT-шейдер, ИИ врагов (уклонения, фланги, отрядное поведение, платформинг).

### Что изменилось относительно первоначальных планов

| Было в планах | Стало | Почему |
|---|---|---|
| Telegram Mini App | Полноценная игра на Godot | Решение пользователя |
| Idle Auto-Battler | Action-платформер | Пивот в сторону Katana Zero |
| Dark fantasy, подземелья | Неоновый ночной город, самураи | Смена визуального направления |
| Спрайты 48×48 | 64×64 | Больше места под детали |
| `attack_speed` 0.35 с | 0.5 с | Итерация баланса |
| `dash_distance` 180 px | 60 px | 180 px было слишком много |
| Откат парирования 15–80 px | 12–55 px | С настоящей гравитацией сбрасывало с платформ |
| HP врага 2 | 3 | Итерация баланса |
| Прыжок «косметический» | Настоящая гравитация 900 | Переписано на реальную физику |

### Что было отброшено

- Telegram WebApp целиком
- Idle/auto-battle геймплей (бои без участия игрока)
- Расстановка бойцов 2×4
- Токен и P2E
- Косметический «хоп» вместо гравитации

---

## 10. Что ещё не сделано

**Направленные атаки.** В `InputMap` есть `move_up` и `move_down`, и по задумке `W + ЛКМ` = удар вверх, `S + ЛКМ` = удар вниз. **В коде этого нет** — телеметрия пишет только `"FORWARD"`. Парирование сейчас работает чисто по совпадению тайминга, направление не учитывается. Это заметное расхождение между дизайн-документом и реализацией.

**Зоны хитбокса.** Обсуждались три зоны (WHIFF / ACTIVE / SWEET SPOT с разным уроном). Реализован один прямоугольник 50×60 без градации урона по дистанции.

**Диалоги.** `DialogueManager` подключён, но ни одного диалога в игре нет.

**Офисное здание.** Многоэтажный интерьер с лестницами — в работе, геометрия требует пересчёта.

**Известная проблема баланса.** Окно контратаки после парирования (стан 18 кадров = 300 мс) короче времени атаки игрока (0.5 с = 30 кадров). Попасть в стан **невозможно** при текущих значениях. Либо увеличивать стан, либо ускорять атаку, либо принять что парирование — только защитный инструмент. **Требует проверки телеметрией.**

---

## 11. Правила работы над проектом

- Пользователь не программист — объясняй кратко, словами, без жаргона
- Код только GDScript, Godot 4 синтаксис
- Всегда говори какие именно ноды/кнопки нажать в редакторе Godot
- Итерация важнее совершенства: рабочий прототип → потом красота
- Не более 2–3 параметров баланса за раз, иначе непонятно что сработало
- Проверяй изменения запуском игры и скриншотом
- Если задача большая — сначала план одной строкой, потом реализация
- При конфликте с решениями выше — скажи об этом прямо, не молчи

**Живая настройка.** Любой новый боевой параметр стоит сразу выводить в админ-панель слайдером — так его можно крутить в игре, а не пересобирать сцену.

**Телеметрия — источник истины.** Прежде чем менять баланс «на ощущениях», посмотри что показывают логи. Комментарий в `player.gd` про тир BARE — прямой пример: телеметрия показала, что BARE это ~86% всех успешных парирований, из-за чего у самого частого исхода вообще не было визуального фидбека.
